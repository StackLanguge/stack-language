`randomness import
`thread import

{
    random print
    0.3 wait
    randomloop tstart
} `randomloop set randomloop tstart

{
    '-- SECOND --' print
    1 wait
    second tstart
} `second set second tstart
